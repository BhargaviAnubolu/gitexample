/**
 * Importing Total Memory file to calculate total memory
 */
 const TotalMemory = require("./total_memory.js");  

 /**
  * importing  copy of the file in directory with the name "destination" 
  */
const copyFile = require("./copyfile");

/**
 * Requiring total number of words in the file
 */
const TotalWords = require("./no_of__words.js");

/**
 * Requiring total number of files 
 */
const TotalFiles = require("./no-of_files");

/**
 * Requiring total number of lines in the file
 */
const TotalLies = require("./no_of_lines");

/**
 * Requiring  highest occurances in the file
 */
//const HighestOccurances = require ("./highest_occurance.js");